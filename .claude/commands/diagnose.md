---
description: "Diagnose a build or runtime error without auto-fixing"
---

Use the build-error-resolver agent to diagnose the following error:

Error context: $ARGUMENTS

Requirements:
1. Run the relevant build/check command
2. Identify root cause of each error
3. Explain why each error occurs
4. Suggest fixes but do NOT apply them — let the user decide
